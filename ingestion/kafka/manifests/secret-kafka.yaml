apiVersion: v1
kind: Secret
metadata:
  name: file-rouge-user
  namespace: kafka
type: Opaque
data:
  # Les valeurs doivent être encodées en base64
  username: ZmlsZS1yb3VnZS11c2Vy       # "file-rouge-user" encodé en base64
  password: c2VjdXJlUGFzc3dvcmQ=       # "securePassword" encodé en base64

# -----------------------------
# Exemple de référence Vault (optionnel)
# -----------------------------
# Si tu utilises HashiCorp Vault et External Secrets Operator :
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: file-rouge-user
#   namespace: kafka
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-secretstore
#     kind: SecretStore
#   target:
#     name: file-rouge-user
#     creationPolicy: Owner
#   data:
#     - secretKey: username
#       remoteRef:
#         key: kafka/file-rouge-user
#         property: username
#     - secretKey: password
#       remoteRef:
#         key: kafka/file-rouge-user
#         property: password
